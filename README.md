# Як закинути на github - завди працюють через git bush here
```
git status
git init
git status
git add .
git status
git commit -m"Init repository"
```
# Створюємо проект на github і беремо посилання з GitHUB
```
git remote add origin https://github.com/novakvova/Sharp_11KN.git
git branch -M main
git push -u origin main
```